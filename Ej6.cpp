/*
 * Utilizando esta estructura, crea un programa que gestione una lista circular de contactos.
 * Cada contacto contiene un nombre (cadena de caracteres) y un número de teléfono (cadena de caracteres).Tareas
 * 1. Agregar Contactos: ○ UsalosmétodosdeCircListparaagregar varios contactos a la lista.
 * Deberás insertar al menos cinco contactos con nombres y números de teléfono distintos.
 * 2. Mostrar Contactos: ○ Implementa una función que recorra la lista circular y muestre todos los contactos en orden.
 * Usa el método imprimir() de CircList.
 * 3. Buscar un Contacto por Nombre: ○ Implementa una función que busque un contacto por su nombre.
 * Usa el método getDato(int pos)paraacceder a los datos de los nodos y busca el contacto en la lista.
 * 4. Eliminar un Contacto por Nombre: ○ Implementa una función que elimine un contacto por su nombre.
 * Deberás usar los métodos getDato(int pos) y eliminarPorValor(T valor)(supón que eliminarPorValor está implementado, aunque no esté explícito en el código proporcionado).
 * 5. Contar Contactos: ○ UsaelmétodogetTamanio()paracontar el número de contactos en la lista y muestra el resultado.
 */

#include <iostream>
#include "Lista/Lista.h"
using namespace std;